extends layout.pug 

block content 
    h2 edit: #{video.title} (⌐■_■)
    form(method="POST")
        h4 change title of video
        input(name="title" type="text" placeholder="video title" value=video.title required)
        input(type="submit" value="save")
